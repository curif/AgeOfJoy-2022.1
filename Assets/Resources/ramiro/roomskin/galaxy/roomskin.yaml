version: 1
kind: roomSkin
displayName: Example room skin

texture: galaxy.png

# surfaces: MRUK anchors to apply this texture (one or more entries).
# Accepted values:
#   floor        — floor
#   walls        — walls
#   ceiling      — ceiling
#   table        — table
#   couch        — couch
#   bed          — bed
#   door         — door frame
#   window       — window frame
#   global       — global mesh

surfaces:
  - ceiling
